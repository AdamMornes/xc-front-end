export default function init() {
    console.log('Sample 2 script initialization');
    console.log('%cIn the demo, non-Vue based scripts will need to be loaded after Vue router, and re-initialized after Vue router page navigation.', 'color: red;');
}
